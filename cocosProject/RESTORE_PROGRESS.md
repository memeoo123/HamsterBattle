# Cocos Restore Progress

> 总阶段以项目根目录 `ORCHESTRATION_STATE.json` 为准；本文件记录 `cocosProject` 的实现状态。

## 当前基线

- 目标：`wxf9af2417e78ce07a/18`
- 代表关卡：`1004 / 荒漠沙地`
- 原引擎：Cocos Creator `3.8.2`
- 还原工程：Cocos Creator `3.8.8`
- 设计分辨率：`750 × 1334` 竖屏

## 当前状态摘要（2026-08-10）

- 总编排已完成：机制数据、Golden、资产导入、TypeScript 与最终视觉基线全部通过。账号成长/关卡推进和 200 关运行时已合并到同一工程。
- 版本化账号档案已接入英雄解锁、1–20 星、挑战次数、资源/碎片、关卡奖励、正式升星与通关推进；真实目标账号数值仍需外部证据，不影响规则闭环。
- 200 关机制数据闭环现已覆盖生产状态机：`1001..1200` 全部 `2,978` 波、`54,816` 个刷怪项逐关验证，所有中间波进入下一准备阶段、末波进入胜利；每关也验证失败计数、重试回到第 1 波并保留连败补偿，以及胜利清零。`CangshuGame` 直接消费同一套纯 reducer。新会话默认 `challengeTimes=1`，优先使用恢复的首次挑战静态候选。
- 生产形状已从场景常量抽成共享纯数据，渲染、占格、合成、核心接触和测试同源。首次动态发牌的全部 `1,728` 种三英雄原始组合以及 16 个静态首批关卡均证明至少有一个核心可连接生产者；H06 2×2 无法单独落入中心占用的初始 3×3 风险因此不会形成开局死锁。
- 刷新现有 Creator 7456 预览后，1001/1100/1200 当前源码实机冒烟全部通过：关卡与 5/15/15 波正确，均完成真实拖放/开战、6/6 次生产应用和 0 缺失配置。
- 1001 连续正式交互会话已覆盖固定批次、生产、特质、6 次失败/结果页重试、机制优先补偿、全部 5 波与最终胜利。胜利时失败计数从 6 清零，最终 569/700 HP、45 金币、879/879 次生产应用，缺失配置和运行错误均为 0；未修改战斗数值、阶段、波次或结果状态。
- 1100 连续正式长跑已覆盖完整 15 波。旧 10% 长期补偿下限在探索运行 25 次失败后仍未可靠收敛；现保留恢复原表与前 3 次精确补偿，仅将重建专用长期下限降到 1%。新构建从零累计 17 次失败后，通过正常拖放、连通块布阵、合成、特质与战斗进入 `won` 并清零失败计数；获胜段峰值己方 9、敌方 22，0 运行错误、0 缺失生产配置。
- 机制/数据自动规则回归全部通过：战斗内核 `116/116`、生产时钟 `32/32`、全关卡数据导出 `3383/3383`、准备表驱动 `411/411`、候选运行配置 `71/71`、依赖矩阵 `1027/1027`、特质表现 `21/21`；主流程和候选关卡浏览器契约均已有通过证据，当前 golden 文件 `47/47`。编排 `mechanicsData` 已通过；账号成长、广告回调及外围交互高保真项目已按用户范围移入根目录 `TODO.md`，不再阻塞视觉阶段。
- 普通关运行数据已升至 v2，200 关逐关导出 `initRewards/staticBuffs/staticBricks`、逐波导出 `coinRewards`；正式加载器已删除仅覆盖 1001/1004 的 `PREPARATION_CONFIGS`，统一消费恢复表。依赖矩阵现为 `200 verified / 0 blocked`，全部关卡已进入公开选择；背景缺失仅使用机制安全的显示回退。
- 已完成战斗帧阶段、同帧刷怪/弹丸/销毁、准备到战斗连续核心相位，以及原生坐标随机和种子战斗随机分域；证据见 `targets/wxf9af2417e78ce07a/18/evidence/runtime/battlefield-frame-rng-order.md`。
- Creator 3.8.8 Web 构建已在本地浏览器完成拖放、开战、三侧连通生产、失败和重新挑战验证，未发现项目控制台错误。
- `1002 / 密林深处` 与 `1003 / 荒漠边缘` 已通过受限候选 URL 进入正式生产场景；新鲜 Web Mobile 构建分别确认 8/10 波、3 组静态批次、准备态拖放及第 1 波开战。1002 连续正式会话进一步完成 H13 两次合成、H04 替换、P01 生产加速、特质、5 次失败/重试和全部 8 波，第 6 次挑战以 361/1375 HP 正常胜利；证据见 `evidence/runtime/candidate-level-1002-closure-current/`。
- 正式关完整胜负集成闭环已通过 1001、1002 与 1100；1100 进一步证明默认重建账号在重建专用最终保障下可完成 15 波。代表账号 P01/英雄成长仍是原版账号平衡的高保真取证项，不再是“200 关机制优先可玩”的阻塞。
- 编排状态为 `complete / complete`；跨调度器同 tick、同时死亡、待结算弹丸及奖励/EXP 等确定性终局边界均已由源码关闭。

## 本轮已完成

- 准备态全局几何/控制子基线已收口：按去除 88 像素微信宿主栏后的 820→750 归一化坐标复核顶部 HUD、血条、背包板、棋盘、候选和三枚底部按钮；移除遮挡原版货币栏的重建主页按钮，准备期隐藏仅战斗可见的产兵条，恢复 `刷新 / 刷新 / 开战`、15 银币费用、“必出高级齿轮”和“获取格子 ×3”。银币头像同步缩到原图内嵌比例。新鲜 750×1334 Web Mobile 截图无控制台错误，专项 `15/15`；证据见 `evidence/visual/reconstruction/2026-08-09-preparation-controls/`。剩余准备态差异为多格连接片与局部美术细节。
- 从 resources2 恢复并解码缺失的 `bagLikeBuff` FairyGUI 包和 867×102 原始图集，锁定 750×1334 窗口、524×83 标题丝带、172×477 卡片、按钮/提示坐标及四张局部图。随后从 resources3 的 `image/effect` SpriteAtlas 恢复 1024×496 原图和 75 个帧记录，将参考三选一精确映射到 `buff_0027 / buff_0036 / buff_0006`。正式场景现绑定原版标题、卡框、说明底板、推荐角标、蓝紫按钮/播放图标和三张能力图；三段说明进一步改为原表 RichText，恢复绿色强调、原版行组、字体和双层描边。新鲜 750×1334 Web Mobile 截图无控制台错误，表现契约 `21/21`；证据见 `evidence/visual/reconstruction/2026-08-09-trait-richtext/`。多格连接片已按原表旋转与裁切关闭；战斗同步子基线也已关闭，完整视觉门禁仅保留战斗表现级差异。
- 从原运行时补齐 WHEEL 齿轮增加基地最大生命：`allWheelHp` 对棋盘上的 H11/H12/H13 求和，使用星级修正后的英雄 HP 与齿轮等级倍率；放入、取下、替换、合成、随机升级和重开均按原 `HomeUnit` 保持当前生命百分比。H13 基础 300、3 星 `+21%` 得 363，与 1004 基础 500 合成原截图的 `863`，因此目标发展态 H13 星级已由截图和表共同锁定为 3。
- 新增浏览器隔离的 1004 发展态三阶段夹具：准备态精确复现 H13/H03/H02 布局、四侧连通、`0.33/0.16/0.18` 与候选金币 `0.03/s`；特质态精确复现等级 2、法术 +5%、H02 30% 分裂和 H03 30% 冰冻。新鲜 Creator 3.8.8 Web Mobile 构建已保存三张 750×1334 证据，项目控制台无错误；视觉门禁仍因组件几何/卡牌包体美术和战斗时刻未完全 matched 而保持 pending。
- 战斗发展态夹具现按 `100401` 的 1.001/3.001/3.501/4.001 秒刷怪排期冻结在 4.010 秒，稳定得到 M02/M02/M07/M02 四只怪、863/863 HP、同一组 P01/H13/H03/H02 齿轮和 0 个己方单位；原版未播种的出生 Y 随机改为仅查询参数内的截图约束值，正常游玩不变。按原图 88 px 微信宿主头部归一化后，战场分界、HP 条和背包面板顶边分别对齐到约 693/729/772 px。HP 已换用原包 `bagLike/xl_icon.png` 的 `439,374,32×27` 红心帧。普通伤害数字使用 `Font_white2` 精确字形和源 transition；H13 命中进一步按 `B_TZ_1301_2` 恢复 `H13_S1_LOWER / baomihua_hill / pskill01`，在单位层下方与伤害数字同一更新触发。证据见 `evidence/visual/reconstruction/2026-08-10-h13-impact/`。仅战斗齿轮辉光、阴影、层级和动画仍保持 pending。
- 恢复 H03/H08 星 10 穿透激光 `3001_5` 的原版 `skill_jiguang`：resources3 UUID/native version、0.70425 秒时长和 SHA-256 已形成证据清单；按 `soundDelay=0` 在激光施法开始时播放，不误移到 300 ms 行为伤害时刻。Creator 3.8.8 已正式导入，新增专项 `5/5`，全量回归升至 `1001/1001`。
- 恢复 H03/H08 状态表现：星 7 `3001_3 → 3001_bh3 → 3001_bf3` 与星 8 替换 `3001_4 → 3001_bh4 → 3001_bf4` 均在目标身上播放原版 `H28_S2` / `hit_lizi` Spine 3.8.99，动作 `hit`、100×100 和 0.5 缩放按 ModelConfig 接入。独立 30% 冰冻 `3001_2 → 3001_bh2 → 3001_bf2` 现同时播放 `skill_bingfeng` 和原版 `H28_S1` / `hit_binkuai`：`idle` 循环、100×100、随目标移动并按 3 秒 Buff 时长刷新/清理。星 7 按配置播放 `skill_bianxing`，星 8 配置音效为空而保持静音。既有 Creator 会话完成正式导入，专项升至 `19/19`，全量回归升至 `996/996`。
- 恢复 H01 `1001/bh1001` 与 H04 `4001/bh4001` 明确绑定的原版近战攻击音效 `skill_jijian`、`skill_zhuangji`：resources3 UUID/native version、时长和 SHA-256 已形成证据清单，二者均按配置 `soundDelay=0` 在攻击动作开始时一次性播放。两个行为的 `modelId` 均为空，因此没有臆造额外视觉特效。Creator 3.8.8 已正式导入，新增专项 `8/8`，全量回归升至 `977/977`。
- 恢复 H12 基础雷云 `LY_1201 → M_LY_1201` 的原版 `H12_S1`：Spine 3.8.99 三件套、`attack` 非循环动作、目标位置、0.8 缩放均由原表确认；效果施放后沿既有 500 ms 行为延迟结算，并在命中时播放原始 `bullet_leiyun`（约 0.913 秒），不再画占位线。Creator 3.8.8 已正式导入，新增专项 `12/12`，全量回归升至 `969/969`。
- 恢复 H13 基础火炮完整表现链 `TZ_1301 → M_TZ_1301_1 → B_TZ_1301_2`：飞行段保持原版 `H25_S1` 爆米花粒图；命中段新增原版 `H13_S1_LOWER` Spine 3.8.99 三件套、`pskill01` 非循环动作和 `BgLayer` 低层路由。首发及基础弹跳逐段在伤害结算同一更新创建命中特效，不再只有弹体。Creator 3.8.8 正式导入，专项升至 `15/15`；4.010 秒截图、37/37 测试文件、TypeScript 与 137 资源/0 missing meta 均通过。
- 恢复 H11 基础治疗行为 `B_ZL_1101` 明确绑定的 `H11_S1` 原版 Spine：resources3 三件套经 UUID/native version 取证，二进制版本为 3.8.99，动作 `skill01_hit_upper` 已在所选友军位置非循环播放并于完成后清理；原技能配置不含音效。Creator 3.8.8 正式导入后新增专项 `7/7`，全量回归升至 `949/949`。
- 沿 H0705 主技能 `8001` 的行为 `bh8001_1` 恢复 `H22_S1_LOWER`：300 ms 行为延迟、原始 114×350 图集、`1_0..1_5` 六帧 packed rect/offset、186×186 原尺寸、`(0.3,0.2)` 锚点和 0.8 缩放均已接入近战命中点；原表没有技能音效。帧率按 30 FPS 显式推定，待 matched 视频校准。Creator 3.8.8 已正式导入，新增专项 `13/13`，全量回归升至 `942/942`。
- 沿 H08 普攻行为 `7001_11` 恢复明确挂接的下层命中模型 `H21_S1_LOWER`：原始 1076×195 图集、六帧 packed rect/offset、202×201 原尺寸、中心锚点和 1.5 缩放已接入。H02/H03 行为没有命中模型，保持无附加特效；H08 帧率暂按明确标注的 30 FPS 推定，待 matched 视频校准。新增专项 `14/14`，全量回归升至 `929/929`。
- 恢复 H03/H08 共用的原版 Spine 3.8.99 弹丸 `H13_S1`：atlas、texture、skeleton、`idle` 动画和三组 native UUID/version 均已取证并由 Creator 正式导入。H03 `M_FS_3001` 的工程速度由错误的 700 修正为原表 300；H08 `M_ATM_7001` 保持 500，两者均在各自攻击行为帧显现。新增专项 `13/13`，全量回归升至 `915/915`。
- 从 resources3 恢复 H02 通用 `H29_S1` 光弹：原始 74×50 PNG、72×48 packed frame、`(0.5,0.2)` 锚点、X 轴翻转与速度 700 均已取证。H0201–H0203 普攻、分裂射击和弹幕逐发使用该资源，H0204 主技能继续使用 `H29_S2` 篮球；主弹现等待原攻击行为延迟后才显现，命中时刻不再与画面提前错位。专项 `12/12`，全量回归升至 `902/902`。
- 从 resources3 索引恢复 H0204 技能 `2002` 的独立 `H29_S2` 篮球弹丸：压缩 UUID、native version、远程原始文件、43×43 尺寸和 SHA-256 已形成证据链；Creator 正式导入后按原 `41×41` 裁切、`(0.5,0.2)` 锚点与速度 700 接入战斗路径。新增专项 `8/8`，全量回归升至 `890/890`。
- H11 治疗齿轮已完成基础星级纵切：`H1101..H1104` 使用原表 9 点工人进度、
  `1/1.5/2.25/3.375` 攻击倍率和 `ZL_1101`；满 100 后沿 WHEEL 路径等待 0.25 秒，
  选择友方最低生命比例单位，在 200 范围内按攻击 100% 治疗一个目标，并在同次成功
  施法中按攻击 50% 修复基地。纯逻辑模块覆盖取整、封顶、满血友军和无友军边界；H11
  已加入正式动态候选，并保留不计入五个受追踪英雄族上限的原版例外。H11 星 5 的
  `RG_H11_abl01_eff02` 现可把后续技能替换为 `ZL_1103`，保持单位治疗不变并把同次基地
  修复提高到攻击的 100%。星 2 `RG_H11_abl02_eff01` 会把单位满血或溢出治疗转为可叠加、
  伤害优先消耗的护盾；基地覆盖了通用治疗消费者，仍只接受普通回血。默认账号星级仍为 1，
  因此不会向基线泄漏未证明的高星能力。星 7 `RG_H11_abl03_eff01` 虽可抽取并计次，但
  v18 的 `addEffective` 不处理 `HEAL_MORE_TARGER` 且没有旁路消费者，故按原版保留为
  runtime no-op，单位治疗仍为单目标。
- 从本机授权微信缓存恢复并解析原版 `bagLike.bin`：
  - `BagLikeBattlePage` 为 `750 × 1334`；
  - 战场中心为 Cocos `(0, 110)`、尺寸 `750 × 300`；
  - 双方兵营横向锚点约为 `-345 / 345`；
  - 背包为 `7 × 5`、格子 `100 × 100`、容器 `730 × 529`；
  - 刷新、开战、顶部 HUD、倍速按钮的原始组件坐标已进入 `RESTORE_SPEC.json`。
- 从原运行时确认普通主线真实胜负规则：
  - `BattleTrunkChapterVo.enemyHomeHp` 固定返回 `-1`；
  - 敌方兵营是出怪锚点，不是 `4000 HP` 的攻击目标；
  - 每波在排期结束且无存活敌人时胜利；代表关卡最后一波（1004 为第 15 波）清完后整关胜利；
  - 我方 `500 HP` 兵营归零时失败。
- Cocos 实现已纠正为：
  - `750 × 1334` 场景和运行时设计分辨率；
  - 原版战场、兵营、100 像素棋盘与底部操作区锚点；
  - 布阵时显示背包；开战后扩展上方战场、背包下移并常驻，只隐藏候选和准备操作；
  - 表驱动波次排期、Boss、能量核心出兵链、暂停/1.5 倍速、胜败面板；
  - 英雄不再攻击虚构的敌方基地 HP，敌人仍会攻击我方兵营。
  - 刷新只替换 `ChooseCom` 候选栏，不再自动把齿轮塞入网格；候选必须手动拖入背包。
  - 本局第一次普通刷新免费，之后每次消耗 `15` 局内金币；每个准备回合保留一次广告刷新。
  - 准备候选不再在静态批次耗尽后永久重复最后一组：1004 非首次挑战按原版
    `3000/3001/3002/3003/3004` 奖励树逐候选位独立加权抽取；原版允许概率重复，不添加人为防重复。
  - 关卡 1001 前八批静态候选及单格、横二格、纵二格、纵三格占位已接入；`G02/G03` 需拖到未解锁区域才能扩格。
  - 准备阶段已恢复候选栏与背包内的拖拽合成：同阶同类齿轮由 `01 → 02 → 03 → 04`，拖动件被消耗、目标件原地升级。
  - 普通齿轮现在按原版“合成优先、否则覆盖替换”：新齿轮落入已占用开放格时，被覆盖的旧齿轮会整件退回候选栏；背包齿轮拖到无效区域也会退回候选栏，不再强制弹回原位。
  - 候选栏与棋盘统一使用 1.0 倍、100×100 逻辑单格及 84×84 可视面，拖入前后不再发生视觉变大；常规三件候选保持单行，退回件较多时按原尺寸分行，不再通过缩小棋子换取空间。根据 `capture/039a4bd5-73b4-4599-b6ea-207dc3f531fa.png`，准备棋盘整体上移、候选固定在面板下方，产率改用独立深色标签，避免候选侵入底排格子。
  - 2026-08-02 的 750×1334 release matched-capture 复核进一步把准备区起点对齐到竞品去除微信标题栏后的设计坐标 `y=300`；部署网格偏移由 `-40` 修正为 `0`，背包、候选和底部按钮整体不再比原版低 40 像素。
  - 顶部 HUD 已按原 `BagLikeTopItem` 的 750×320 布局重组为暂停、金币、券、EXP、等级六边形、`1/15波`、统计和图鉴；移除重建端独有的关卡标题/目标/阶段大字，并在目标截图状态隐藏倍速按钮。已追踪跨包 ID `38fa8c75` 恢复 `comm` FairyGUI 包，顶部暂停、统计、图鉴和计数器底板，以及底部蓝/绿/黄三枚按钮已改用竞品原始 atlas。`HeaderItem` 运行时证据又确认普通关内绑定 `COIN=5` 和 `AD_TICKET=4`；已按 `item.ItemConfig` 路径从 resources3 恢复 `dj0002_icon` 仓鼠银币与 `dj0005_icon` 橙色观影券，移除灰圆和“券”字替代。
  - 已从 resources3 恢复原始 `TsangerYunHei W08` 的 `default.ttf`，SHA-256 为 `072c8fe3f6d86fad4cf3be45f0613485930d2c2169e4239a1661ef7452b8273a`。FairyGUI 二进制确认并接入 HUD 计数 `#060500/2`、波次 `#0C0C0C/2`、底部按钮 `#000000/3` 三类描边；release 截图 `preparation-initial-original-font-outline.png` 已验证这些准备态可见文字。未访问弹窗的文字仍须逐组件核对，不外推为全局字体表现完成。
  - 已从 Cocos 3.8.8 release web build 采集有效的准备态和真实交互进入的第 1 波截图，清单与逐项差异见 `targets/wxf9af2417e78ce07a/18/evidence/visual/reconstruction/2026-08-02/manifest.json`。2026-08-08 又通过按随机形状规划三件连通生产器，首次在正常拖放/开战流程到达 EXP 特性三选一，证据见同级 `2026-08-08/manifest.json`。特性卡已改用恢复的 `cl1..cl5` 品质齿轮底图、蓝/粉双按钮和确定性中文换行；最新 Creator 3.8.8 `web-mobile` 构建已在 750×1334 浏览器夹具中验证三卡文案不越界、按钮完整且无项目控制台错误。原战斗/特性参考是 863 HP 的发展中存档，而当前为 500 HP 初始局，故仍非 same-state pair，`visualBaseline` 保持未通过。
  - 可生产齿轮不再按英雄系列 `tint` 上色，而是统一按合成等级使用原版色阶：1 级绿 `#378A4A`、2 级蓝 `#3E6FD4`、3 级紫 `#8140CB`、4 级金 `#CB9B40`、5 级红 `#FF6363`。候选、棋盘与合成后的目标件共用同一渲染路径；能量核心和扩格奖励继续使用各自配置色。
  - 已进一步接入原版 `bagLike_0.png` FairyGUI atlas 的 `cl1..cl5`：从包描述恢复精确的五个 `110×110` 裁切矩形，候选和棋盘现在直接显示竞品绿/蓝/紫/金/红齿轮原图。`BagLikeShapeConfig` 对静态多格底板的映射也已接入：横/竖二格用 `panel1`，横/竖三格用 `panel3`，四种 L 形用 `panel2` 和原角度，2×2 用 `panel4`；底板按合成等级着色并置于齿轮下方。
  - 原运行时 `updateGearAni` 已确认动态叠层门槛精确为 5 级。已恢复并导入 Spine 3.8.99 的 `UI10020/21/22`，分别覆盖二格、L 形与 2×2，按 `panelRotate` 播放 `idle`；三格直线和单格原版无此叠层。当前 1004 正常流程只实现同族 1–4 级合成，因此还需恢复跨族五级配方并现场生成五级结果后，才能完成该路径的端到端截图验收。
  - FairyGUI 包解析器已从“仅组件”扩展为完整读取本包 `54` 个组件与 `47` 张图片记录，过去未知的 `grid` 已证实指向 `gezi2.png`。准备区现直接使用原版 `zd1_frame.png` 蓝色操作底、`zd_frame.png` 深色背包面板和 `gezi2.png` 开放格，并按包内 scale9 边界渲染；独立 release 构建截图 `preparation-initial-original-panel-grid.png` 已验证异步帧绑定不会再重置目标尺寸或遮挡顶部 HUD。
  - 新的 release 构建截图 `preparation-initial-original-common-ui.png` 已验证 `comm_0.png` 的裁切、scale9、子节点层级和异步尺寸保持；SHA-256 为 `e5d5bd15ddc5f3814d0da30540d6d63beac813d96d55a3d7e659d15aea7b1579`。
  - `preparation-initial-original-item-icons.png` 已验证银币/观影券裁切、132×132 原始尺寸、trim offset 和计数器层级；SHA-256 为 `e1d038473cb38e056a0b94329b6bf7a29e67e2917c2dba5ba0e1fdd8c0b43436`。
- `preparation-initial-original-connectors.png` 中的两个随机 L 形候选直接验证了 `panel2` 原图、方向、中心、等级着色和层级，且没有重新引入候选重叠；SHA-256 为 `b304254ba755f5276d64e7b2c8d42932e1b29f0fbcc8acaf9ce85137d3302af3`。
  - `preparation-initial-original-font-outline.png` 已验证原始 TTF 在 release 构建中的载入，以及 HUD/波次/按钮三类描边；SHA-256 为 `c26a22306351ccccf5a53d7463cc41b5f086542b92c07479f044f44330b6cc25`。
  - 用户已在 Creator 预览中复验并确认上述候选区尺寸、重叠与棋盘分区问题解决；该结论仅关闭准备区几何子项，不替代完整的四阶段 matched capture。
  - 多格齿轮不再压缩成 24 像素装饰点；每个真实占用单元按 `100 × 100` 网格间距绘制，并逐格显示类型和合成等级。
  - 银币齿轮每次工人进度完成后的产量按原表从 `2 → 4 → 8 → 16` 提升；进度由核心触发，每次增加 3，不再误作“每 3 秒”。
  - 新增独立 `BattlefieldKernel.ts`，战斗统一走原版的闪避半伤、暴击、HAMSTER/WHEEL 抗性分流、Boss 增伤、向下取整与最小 1。
  - 新增独立 `BattlefieldProduction.ts`：能量核心逐侧触发正交连通块，工人进度满 100 才生产；同一连通块接触核心 N 侧时每圈触发 N 次。
  - H01/H02/H04 的 `10/8/6` 已纠正为单次触发进度，不是秒数；开战不立即出兵，完成后经 0.25 秒工人动画和 0.5 秒飞行动画，在 `x=-300, y=random(0,150)` 创建单位。
  - 齿轮等级现已完整传入产兵链：H01/H02/H03/H04 的 1–4 级分别使用原版 `1 / 1.5 / 2.25 / 3.375` 属性倍率、等级头像、等级模型 ID 和模型资源；1–3 级模型缩放为 `0.8`，4 级为 `0.88`。此前缺失的 12 套二/三/四级 Spine 3.8.99 已从 `resources3` 恢复并由 Creator 导入。H0204 的主技能身份切换为 `2002`，并使用区别于低等级 `H29_S1` 光弹的 `H29_S2` 篮球弹丸。
  - 第 11–15 波能力池现已接入原版 `RG_ALL_abl10_eff01`“随机 1 个齿轮升级”：只在棋盘已放物件中排除核心、满级及无 `nextId` 项后均匀随机，候选栏不参与；没有合法目标时保持无效果且不消耗 RNG。命中后保留工人进度与待完成生产任务，原地更新等级颜色、头像和配置，后续产兵/塔技能/金币立即使用升级后的等级链。
  - H12 的 `20` 同样是单次进度；完成后从齿轮位置释放一次随机目标、500 ms 后按 50% 攻击结算、半径 50、最多 5 目标的 WHEEL 技能，不创建常驻 H12 单位。`RG_H12_abl01` 已按账号星级在 1 秒 `LY_1202` 与 2 秒 `LY_1203` 中只保留最高合格版本，麻痹主目标并阻止移动/攻击，控制免疫生效。M03/Boss03 远程技能为 50% 攻击。
  - 普攻行为延迟现在随攻速缩放并按毫秒向上取整；弹丸保存发射时攻击/方向，施法者死亡后仍继续飞行，单体目标提前死亡则落空。
  - 基础移速/索敌范围已纠正为英雄 400、怪物 1200、全局移速 60；H04 `MOVE_SPD=5000` 后为 90；Boss03 `ATK_SPD=5000` 且不再错误继承 M03 的塔易伤。
  - 精确索敌与移动已按原运行时接入：可选存活目标进入双方四叉树，圆形搜索/攻击边界均使用严格 `<`；普通攻击每帧重新选最近敌人，等距时复现四叉树逆序优先；无目标时远离基地 200 像素以上只横移、进入 200 后转为斜向基地，距基地横向 40 内停止，普通主线只有敌军攻击我方基地。移动改为保持总速度的归一化向量，并按“英雄逆序→怪物逆序”更新；英雄另有 60×60 邻域、每个邻居 2 像素的分离推力。逻辑基地坐标恢复为 `±300`，视觉兵营锚点仍保持独立。
  - 连续失败后重试会按原版 15 档表同时降低敌军 ATK/HP，第一、二次重试分别为 95% 和 90.25%。

- 后三种五级融合 `H1005/H1505/H1805` 已从“仅有配方”推进到完整可运行链：恢复并绑定三枚 `heroSmallHead` 精确裁切、原表多格形状、工人进度、H10/H18 仓鼠与 H15 WHEEL 生产消费者；H10 的 `10001_2` 按 6 秒初始冷却、10 秒冷却和 1–5 秒五段全场伤害运行，H18 的 `12001_2` 按 5 秒冷却和六段 `100×300` 前向矩形运行，H15 固有击杀 +1 金币与每个已放置 H1505 回合结束 +16 金币均已接入。H1005 普攻 type 11 已证明会落入默认 `BulletUnit` 而非 Ray：从技能 `(0,50)` 攻击点固定方向发射，速度 1000，在目标初始点前约 20 像素结算一次 `100×300` 矩形伤害，目标提前死亡则不重新锁定；`distance/timeLimit/width/interval/autoLock` 均是该路径未消费字段。新鲜 Web Mobile 真实战斗夹具 `13/13`，记录 5 次 H1005 普攻发射/2 次命中、1 次主动施放/2 次主动命中，三头像 loaded、三进度条 visible、缺失配置 0、控制台错误 0。原始 Spine/技能特效仍属于表现待办。

## 自动校验

- Restore spec ready gate：通过，`implementationReady=true`。
- Golden cases：当前文件 `47/47` 通过，覆盖伤害、波次、基地生命、生产、刷新、摆放、合成、占格、金币、失败补偿和阶段条件；更细规则、资源绑定、主流程与特性表现由 26 个脚本的 `1040/1040` 断言承担。
- 生产战斗内核：`116/116` 断言通过，直接导入 `BattlefieldKernel.ts`，新增覆盖完整战斗帧阶段、同帧刷怪/弹丸/销毁顺序、原生坐标随机与种子战斗随机分域，以及默认种子 5、`9301/49297/233280` LCG；既有伤害、攻速/失败补偿、精确索敌、移动、基地路径、英雄分离碰撞、弹丸命中时的实时攻击/死亡快照边界、技能/Buff/弹射/惰性 RNG 覆盖继续通过。
- 生产机制内核：`28/28` 断言通过，新增覆盖准备到战斗连续核心相位、方向 1 首次完成接触和占用侧 200 ms 停顿；既有多侧接触、连通块、产率、100 点完成、余数保留、输出延迟、1.5 倍速、核心直接相邻与“仅连通不相邻”的区分，以及相邻效率提升的小数进度和显示率继续通过。
- Cocos 静态检查：`128` 个资源文件、缺失 `.meta` 为 `0`、警告 `0`；H03 激光/变形/冰冻音效与状态 Spine、H01/H04 原始攻击音效、H0905 原始效果、原字体、12 套等级模型及 3 套五级形状叠层保持有效。
- TypeScript：使用 Creator `3.8.8` 工程声明并按技能约定加 `--skipLibCheck true`，项目脚本检查退出码 `0`；不跳过库检查时仅出现 Creator 自带 `cc.d.ts/jsb.d.ts` 环境声明错误。
- 当前全量 30 个机制/数据/资源脚本为 `5949/5949`（含主场景/关卡选择纯逻辑 `31/31`、特性表现/原版图集夹具 `17/17`、兵种等级与 WHEEL 基地生命 `276/276`、治疗/护盾/H04 周期恢复 `42/42`、特性池 `240/240`、H12 替换/状态 `15/15`、战斗内核 `116/116`、生产时钟 `31/31`、准备动态发牌 `40/40`，以及各已恢复原版资源绑定）；主流程真实画布契约另有 `9/9`，当前 golden 文件 `47/47`。
- 本轮检测到用户既有 Creator 3.8.8 工程会话 PID `4363`；仅唤醒该会话触发资产刷新，未启动第二实例、未终止既有进程。H03 激光音频导入后资源检查为 128/0。
- 本轮打开工程前未发现既有 Creator 进程；只启动并停止了本轮 Creator 3.8.8 会话。资源数据库 ready，扩展商店 `ECONNRESET` 不影响本地音频导入。
- Main 启动黑屏回归已定位并修复：兵营节点不再同时挂载 `Sprite + Graphics`，标题节点不再同时挂载 `Label + Graphics`；血条改为独立子节点。
- `capture/log/8ff1cc4c-a1c4-4b96-ac05-85692f38058a.png` 中的 `BarFilled.updateUVs` 空帧异常已定位到头像进度遮罩：现在先绑定有效 `SpriteFrame` 再启用 `FILLED`，且 `refreshUi` 仅在帧与纹理就绪、进度实际变化时更新 `fillRange`。TypeScript 与 6 组回归测试已通过；当前 Creator 预览缓存仍是 11:56 的旧脚本，需停止并重新预览后完成运行时复验。
- 显式加载 Main UUID 的预览已显示完整布阵首屏，修复映射下没有新增可渲染组件警告；当前已打开的 Creator 预览仍需停止后重新运行以换入新脚本。
- 1004 场景测试：`28/28` 断言通过，覆盖 15 波、Boss 顺序、准备棋盘/候选分区、战斗背包布局、EXP 阈值、截图三能力 ID，以及 1.5 倍 EXP 的小数余数与单级升级语义。
- 特性随机池：`240/240` 断言通过，覆盖原表权重、不重复、阵容/账号星级过滤、同组最高星级版本、次数上限、普通升级无品质保底、仅“换一批”品质 4 保底、推荐标记、属性作用域、随机齿轮升级、`RG_ALL_abl11_eff01` 的精确 3012 no-op、经验倍率、全敌军减攻、核心相邻齿轮的攻击/效率提升、两种基地治疗、H01 连击必暴四版本及星 7 最后一击叠攻、H02 星 3/5/10 分裂射击与星 7/8 弹幕、H03 星 7 眩晕变形、星 8 `DMG_INC` 替换档与星 10 穿透激光、H04 星 2/3 骑士活力、星 7/10 盾墙及星 8 H04/H09 普攻击飞、H11 星 2 护盾/星 5 基地修复及星 7 分派断链 no-op、H12 全部四组能力，以及 H13 星 2/3 弹射次数和星 7/10 爆米花替换版本。
- 准备候选动态规则已改为 `40/40` 断言并接入正式发牌：账号已解锁英雄族可由场景属性配置；棋盘金币齿轮按 `1/2/4/8` 计数并套用原表 `3034` 权重衰减；同时记录的英雄族达到 `5` 后不再抽入第六个受追踪族，H11 按原版例外仍可抽取；未满五族时可把重复候选替换为同等级的已解锁缺失族；仍有锁格时，每第 `7` 次非广告刷新若本批没有扩格，会把第 3 个候选强制替换为 `3030` 扩格奖励。
- 准备摆放与空间：`24/24` 断言通过，直接导入生产模块，覆盖边界、核心保护、单/多齿轮整件退回、移动件旧占格忽略、候选/棋盘同尺寸、候选溢出分行、1–5 级精确色值、按等级覆盖兵种色和无等级特殊件回退色。
- 兵种等级联动：`276/276` 断言通过，覆盖 H01/H02/H03/H04 全部 1–4 级及 H0705/H0805/H0905 三种融合兵的精确倍率、头像、模型 ID、资源路径、模型缩放、主技能编号、H0204 技能切换，H11/H12/H13 一次性塔技能的等级倍率/技能身份，以及完整 1–15 星 WHEEL 基地生命与齿轮倍率链。
- 跨系列五级融合：六组原表配方及双向材料匹配、`HERO_STAR_GE` 门槛已固化，规则测试 `10/10`。当前已恢复兵种链可正常到达 H0705（H0104+H0204）、H0805（H0304+H1204）、H0905（H0404+H1304），合成后会按新形状重新检查空间，越界则退回候选区、新覆盖件整件退回；星级不足不会消耗或替换材料。三套原始 Spine 3.8.99 已导入并连接产兵/基础战斗。目标账号星级尚无证据，属性默认 1。H0905 的 type-8 已由运行时确认为 `BounceBullet`，不是同目标双发：首发后最多再弹射 2 次，每次在 300 范围内选最近且本链未命中的敌人，每段 35% 攻击、速度 400。现已接入 `H24_S1` 原始飞弹、`H24_S1_LOWER` 三帧命中图和 `bullet_zhanche` 音效；精确帧几何已确认，三帧速度暂按 30 FPS 推定，仍待 matched replay。
- 五阶融合实机夹具：显式 `fusionValidation=tray|placed|battle` 参数下已采集 750×1334 的候选区、棋盘摆放和产兵截图；正常 URL 不启用夹具。日志确认 H0705/H0805/H0905 进入正式生产队列，分别生成 R1001/R1002/R1003，三套 Spine 均加载成功。夹具同时发现并修复战斗阶段 `BackpackPanel` 未随网格下移、遮挡战场的回归。当前 TypeScript、101 资源/0 missing meta 通过；该证据不代表未知账号星级，也不能替代竞品 matched capture。
- EXP/特性流程已接入生产脚本：普通怪 5 EXP、Boss 100 EXP，20/50/100 阈值；升级暂停并显示三张能力。经验强化选择后只把后续 EXP 通知乘 1.5，保留原版一次通知最多升一级和小数余数。
- 敌方攻击弱化已接入战斗路径：每次选择对普通怪、精英和 Boss 的实时攻击统一减 5%，最多十次；已经生成的敌人立即生效。在途弹丸按竞品规则优先读取存活施法者实时攻击，施法者离场后才使用发射快照。
- 动力核心相邻攻击强化已接入生产路径：只有实际占用核心上下左右直接邻格的齿轮获得 20% 攻击提升，连通但未直接接触的齿轮不获得；多格齿轮任一格直接接触即整件生效。它只提高后续产出仓鼠或 H12/H13 一次性技能的攻击，不提高 HP，也不追溯修改已生成仓鼠。
- 动力核心相邻效率强化已接入生产路径：`RG_ALL_abl16_eff01` 让同一直接邻格范围内的齿轮每次核心触发进度与 `/s` 显示同时乘 1.2，并保留小数余数；候选区和仅连通齿轮不获得加成，核心转速与产出单位属性不变。
- 低血量即时基地治疗已接入选择路径：`RG_ALL_abl17_eff01` 只在基地生命比例 0%–50%（含端点）时入池；选中后立即恢复最大生命的 50%，向下取整并封顶，同时显示治疗数字。原表 `noRestore=1` 已保留，因此它不进入永久特性栈，后续低血量时仍可再次出现。
- 每轮开战前基地治疗已接入回合开始路径：`RG_ALL_abl18_eff01` 只在基地生命比例 0%–75%（含端点）时入池，最多选择一次并持久化；选中时不立即治疗，之后每次开战通知恢复最大生命的 10%，向下取整、封顶并显示治疗数字，回合结束不触发。
- H01 连击必暴已接入未来产兵与伤害路径：`RG_H01_abl02` 按账号 H01 星级 `3/5/8/10` 只让同组最高版本入池；运行时分别完成 `3/3/2/2` 次基础攻击后令下一次未被闪避的攻击必暴，后三版额外增加 5000 暴伤，十星版在触发时回满。能力只进入选择后新生成的 H01，已在场单位不追补；闪避优先且不会消耗必暴。目标竞品账号的 H01 星级不在安装包和现有截图中，因此 Creator 属性 `h01HeroStar` 暂守证据默认值 1，不让未证实的紫卡进入基线。
- H12 高压电击已接入一次性雷云技能：`RG_H12_abl01` 的星级 1/3 两条分别替换为 `LY_1202/LY_1203`，只让最高合格版本进入池；对随机主目标施加 1/2 秒 `dizziness`，阻止移动和攻击并尊重控制免疫，原 500 ms 后半径 50、最多 5 目标、5000 比率的区域伤害保持不变。`h12HeroStar` 默认 1，因此基线启用有证据的一秒版，高星账号值仍待外部证明。
- H12 十万伏特已接入暴击属性链：`RG_H12_abl02_eff01` 要求 H12 2 星，按原表向 H12/H08 注入 `CRI_RATE=10000`，非闪避的雷云伤害必定使用 15000 基础暴击倍率；默认 1 星基线不会出现该卡。伤害 RNG 改为原版惰性顺序：闪避成功不取暴击随机数，一次性强制必暴也短路暴击随机数，属性必暴在未闪避后仍正常取一次暴击随机数。
- H12 百万伏特已接入暴击伤害属性链：`RG_H12_abl03_eff01` 要求 H12 7 星，向 H12/H08 注入 `CRI_DMG=5000` 并按属性表封顶 25000；它不自行增加暴击率，只把实际暴击的倍率从 15000 提高到 20000。H12 的 49 攻击、5000 技能比率命中时由 36 提高为 49；默认 1 星基线不出现此卡。
- H12 感电效应已接入同组技能替换链：`RG_H12_abl04_eff01` 要求 H12 10 星、品质 4、权重 50，替换为 `LY_1204`。它与麻痹版本同属 `LY_1201` 组，因此严格保留“最后选择者覆盖先前替换”的顺序；`LY_1204` 不带麻痹，并对主目标施加常驻、单层 `DMG_RES=-1000`。原属性读取下限为 0，关卡 1001/1004 当前目标的通用 `DMG_RES=0`，故不臆造 10% 易伤数值，但保留其覆盖麻痹的真实交互。
- H13 玉米弹射已接入 WHEEL 弹丸链：基础 `TZ_1301` 恢复 type-8 `BounceBullet` 的 2 次后续弹射、300 严格半径和共享已命中集合；`RG_H13_abl01_eff01/02` 按 H13 星 2/3 只让最高版本入池，并通过 `FEATURE/BOUNCE_TIMES +2/+4` 将 H13/H09 的后续弹射上限提高到 4/6。默认 `h13HeroStar=1` 只保留基础链。
- H13 爆米花技能已接入同一正式弹丸链：`RG_H13_abl02_eff01/02` 按 H13 星 7/10 只让最高版本入池并替换为 `TZ_1302/TZ_1303`；两者让后续弹射攻击逐段按 1.1 复利，初击不变。`TZ_1303` 配置的末跳范围爆炸因原版子弹先重置计数并判断 `last_missile`、之后才复制父弹计数而不可达，工程按版本 18 保留该 no-op。
- H02 分裂射击已按原被动链重接：`RG_H02_abl02_eff01/02/03` 按 H02 星 3/5/10 只让最高版本入池，在主技能动作前以 30%/50%/100% 判定，并从射手自身半径 250 内随机选择一个敌人（允许主目标）发射独立速度 700、100% 伤害弹体；旧的主弹命中阶段固定 30% 最近非主目标近似逻辑已移除。
- H04 骑士活力已按运行时而非卡牌文案接入：星 2/3 只让最高版本进入 `RG_H04_abl02`，后续生成的 H04/H09 单位在首个 1 秒后每秒按自身攻击力 2%/5% 向下取整回血并封顶；一级 H04 实际为每跳 1/2 HP，冻结不暂停被动计时。
- H01 叠中叠已按死亡事件真实消费者接入：星 7 选中后，仅 H01/H07 完成最后一击时为共享计数加一，并给双方家族实时攻击增加 2%，最多 30 层；卡牌“参与击败”的宽泛文案不用于模拟助攻。
- 三个截图能力已接入战斗路径：法术齿轮攻击 +5%、射手 30% 额外目标、法师 30% 冰冻 3 秒。
- 升级特性不再固定显示截图中的同三张牌：当前池含 53 条原表效果行，对应 41 个互斥能力组（39 个有效组和 2 个版本配置/分派断链的确认 no-op）；每次升级和“换一批”都按原始 `weight` 加权不重复抽三张，并过滤次数上限、当前已摆放英雄、账号星级、基地实时生命和已恢复的波次条件；普通升级不设品质保底，只有“换一批”保证至少一张品质 4（紫色）。
- 已从 `resources3` 原始 bundle 恢复 H0401 的 `js_qishi_1` Spine 3.8.99（attack/die/idle/move，scale 0.8），替换骑士的圆形回退图。

## 新增原版视觉证据（2026-08-01）

- 用户提供的 4 张截图已经隔离保存到目标目录
  `evidence/visual/original/2026-08-01/`，覆盖初始准备、发展后准备、第 1 波战斗和
  “选择激活特性”弹窗。
- 截图对应 `1004 / 荒漠沙地`，依据是荒漠 `fightscene_03`、`1/15波` 和初始
  `500 HP`；当前工程却硬编码 `1001 / 宁静森林` 与五波，因此此前比较基线错配。
- 原版开战不会隐藏背包，而是扩大上方战场并将背包保留在下方；当前
  `prepareLayer.active = false` 是最早的 P0 结构偏差。
- 原版战斗内 EXP 升级与三选一特性系统已恢复随机权重抽取、阵容/次数/账号星级过滤，并按原分支仅在“换一批”时执行品质 4 保底；
  当前已表现英雄族相关能力效果池已完整接入；玩家英雄星级验证与广告播放仍待接入。
- 合成/占格逻辑已有，但原版使用异形完整齿轮美术、仅强调开放格，并显示 `/s`
  生产率；当前逐格绘制仍无法通过视觉验收。

## 完全复原验收口径与剩余工作（2026-08-02）

当前成果是 `1004 / 荒漠沙地` 的主要可玩闭环，不等同于竞品关内系统已经完全复原。
以下内容保留为 2026-08-02 的历史验收清单。当前总编排已经完成，`mechanicsData` 与 `visualBaseline`
均为 `pass`；未关闭项目属于目标账号精确存档、平台广告和逐帧表现等非阻塞高保真扩展：

1. **精确索敌与移动（规则恢复、代码接入及确定性测试已完成）**
   - 已恢复存活/可选过滤、最近目标与等距四叉树决策、逐帧重新索敌、严格搜索/攻击半径、
     归一化移动、英雄分离碰撞、基地远近路径、停止距离与双方逆序更新。
   - H01–H04 和代表关卡怪物走最近目标链；H12/H13 的随机目标、近远程攻击、固定落点范围弹丸、
     施法者/目标死亡与兵营分支均已分别核对。规则证据见
     `targets/wxf9af2417e78ce07a/18/evidence/runtime/battlefield-targeting-movement.md`；现场轨迹对照仍归入第 6 项 matched replay 总门禁，不再作为本子项的未知规则。
2. **完整技能、Buff 与状态池**
   - 原 `BagLikeAbilityEffectConfig` 有 78 条抽取效果；53 条可能作用于当前已表现的
     H01/H02/H03/H04/H11/H12/H13，工程已全部建模为 41 个互斥能力组（实际每次资格仍受波次、阵容和星级条件影响）。
   - 随机齿轮升级、二级齿轮准备刷新配置断链、经验强化、全敌军减攻、动力核心相邻齿轮攻击/效率强化、两种基地治疗，以及当前英雄族的添加/替换主动技能、被动和特殊词条均已恢复。战斗控制器同帧顺序也已固定；后续缺口集中在外部账号/条件数据、跨调度器边界和表现反馈，不再是已知效果行缺失。
   - 需接入玩家英雄星级验证、剩余 condition/verify 处理器，并恢复“换一批”和“全都要”
     的真实广告流程。Boss03 的 `BT_001` 已通过 H12 麻痹与 H03 星 7 眩晕两条实际可选控制链验证：保留技能伤害/变形时长，只拒绝控制状态。
3. **准备阶段剩余动态规则**
   - 账号英雄解锁过滤、金币数量临时权重、最多五种英雄替换和每七次非广告刷新的强制扩格节奏已接入；目标账号的精确解锁集合仍待存档/英雄页证明。
   - H11 基础治疗、星 2 护盾、星 5 基地修复与候选例外，H12 四组能力、H13 玉米弹射次数/爆米花替换，以及 H03 星 10 穿透激光已完成；H11 星 7 `HEAL_MORE_TARGER` 已证实为 v18 分派断链 no-op。
   - 跨系列/五级融合的六组配方、星级门槛、前三种可达融合兵和合成后占位处理已恢复；仍需取得目标账号星级并完成前三组正常流程实机验收，后续随 H05/H06/H14/H16/H17 恢复启用其余三组。
   - 继续验证每回合刷新计数、覆盖退回、非法落点、合成位置等完整实机交互。
4. **时间轴、随机与持久化**
   - 核心首次完成四分之一圈的方向、准备到战斗连续相位、战斗帧的刷怪/单位/弹丸/胜负顺序已恢复；到期弹丸、基地死亡优先、最后击杀、同步 EXP、立即清弹及 1000 ms 回合奖励边界也已按原包关闭。公共包证明两个 Timer 有效注册优先级相同，启动器先创建 `GameTimer`，因此同 tick 固定为核心生产先、`BattleTimer` 战斗帧后；正式运行时和源码契约测试已锁定该顺序。
   - 出生 Y 与工厂 X/Y 抖动保持原生随机，命中/暴击/随机目标等使用已恢复的独立种子流；同归于尽和未结算弹丸终局已有确定性契约。
   - 已确认未完成战斗断点可保存/恢复 `failedTimes`，但失败、战斗结束和退出均会清除断点；外部账号层 `getFailMultipleCfg` 不作为战场跨进程本地存档复刻。仍需确认是否还有会打断战斗的其他弹窗阶段。
5. **原始视觉、动画与声音**
   - 等级颜色语义、原版 `cl1..cl5` 齿轮 atlas、静态多格连接板、开放格、HUD/按钮 atlas、准备态已确认的字体/描边，以及五级 shape `modelId` 动画资源/渲染路径均已恢复；前三组跨族五级配方和融合英雄原始模型已接入，仍需用有证据的星级触发叠层实机验收、恢复品质边框，并继续核对未访问弹窗的字体、背景裁切和精确布局。
   - H01/H02/H03/H04 的等级模型及配置缩放已接入；H02 低等级光弹、H0204 篮球弹丸、H03/H08 共用 Spine 弹丸及 H08 六帧命中均已使用原始资源；H03/H08 激光已绑定原始 `skill01` Spine 动画、数值射线和 `skill_jiguang` 音效。仍需校准实机朝向、排序、动画速度，并补齐其余兵种弹丸、命中、技能、状态、伤害/暴击/闪避文字、胜败/奖励反馈及其余关内音效。
6. **完整实机闭环与 matched replay**
   - 2026-08-09 新 Web 构建已真实完成 1002 的静态发牌、候选拖放、H13 合成、H04 替换、开战、特质、5 次失败/重新挑战及全部 8 波。Creator Release 的 Set 展开错误已修复；第 6 次挑战无人工改值胜利，终局为 361/1375 HP、80 金币、失败计数清零，产兵缺失配置与运行错误均为 0。
   - 下一长跑目标改为 15 波中/晚关；代表账号 P01 等级、英雄星级/解锁族/起始状态只用于原版账号平衡高保真复现，不再阻塞 1002 的完整胜利门禁。
   - 对齐竞品的分辨率、阵容、波次、速度和时间点，采集初始准备、发展后准备、第 1 波、
     特性选择四组重建截图，并比对出生、目标、命中、HP、金币和阶段事件轨迹。
   - 从第一处时间或画面分歧开始修正；编译通过、单张截图接近或数值测试通过均不能替代该门禁。

若“完全复原”指竞品全部关卡而不只是 1004，则还需在 1004 通过上述门禁后，将范围扩展到
已解析的约 200 个关卡，逐关覆盖专属准备配置、英雄/齿轮/怪物/Boss、场景、波次、特殊模式、
技能和资源；目前尚不能把 1004 的代表关卡结论外推为全关卡完成。

建议执行顺序（机制和数据优先）：当前英雄族相关效果行、战斗帧顺序、核心连续相位和 RNG 分域已接入，下一步完成十五波胜利与失败/重试实机闭环，并在可得证据下关闭跨调度器同 tick 边界；美术音效、像素级布局与 matched visual 放在机制门禁之后处理。

## 仍需现场确认

- 在当前 Creator 窗口预览 `Main.scene`，采集 1004 初始准备、发展后准备、第 1 波战斗和特性弹窗，形成 matched pair。
- 实际完成一次“布阵 → 15 波战斗 → 胜利面板”的点击冒烟测试。
- 尚无同关卡、同阶段、同时间点的“原版 + 重建”完整配对；准备态确认字体已恢复，但战斗/特性中的单位比例、动画、层级和未访问文字仍不能标记为像素级一致。
- 准备掉落的账号过滤输入、金币数量动态权重、五英雄类型、H11 例外和强制扩格已接入；仍缺目标账号的精确解锁/星级数据、五级合成正常账号路径，以及最后阶段的状态视觉与声音。

## 交接快照（2026-08-02）

- 当前用户优先级为机制和数据；视觉/音效/精确布局暂缓。
- `1004` 机制和数据规划估算约完成 `75%–80%`；全约 200 关系统约完成 `50%–55%`。估算不替代门禁状态。
- H11 星 7 `HEAL_MORE_TARGER` 已确认是 v18 selectable-but-inert 配置断链；H01 星 7 最后一击叠攻、H02 星 3/5/10 分裂射击与星 7/8 弹幕、H03 星 7/8 变形与星 10 鼠鼠激光、H04 星 2/3 周期自疗、星 7/10 盾墙和星 8 H04/H09 普攻击飞已完成。当前英雄族相关 53 条效果行均已进入真实消费者或证据确认的 no-op，下一切片收敛同帧/RNG 顺序和十五波完整闭环。
- 完整接手说明、验证命令与禁止误判项见目标目录 `MECHANICS_DATA_HANDOFF.md`。

## 200 关机制/数据优先扩展（2026-08-09）

- 主关卡选择已从 `1001/1004` 扩展为恢复表中的连续 `1001..1200`，使用每页 10 关、共 20 页的功能性选择界面；直接战斗参数也接受全部 200 关。
- `NormalLevelRuntime.ts` 将 29 条怪物目录行转换为独立于美术的机制 profile。200 关实际引用的 25 个敌人 ID 已全部解析；近战/远程/超远程、扇形/溅射、英雄/齿轮抗性、Boss 攻速/移速/免控、专注基地、自爆、击退、M14/Boss14 刺杀、B01 三发普攻及 B01/B02/B03 主动 Boss 技能均已进入正式运行路径。证据见 `evidence/runtime/normal-level-enemy-specials.md`。
- `fightscene_02/04` 不再阻止进入关卡：保留原背景 ID，资源缺失时只在显示层回退到 `fightscene_01`。这是明确的表现降级，不改变波次或战斗数值。
- 新增全关契约验证：200 关、2,978 波、54,816 个排期刷怪项均可构建且每个敌人能解析机制 profile。补齐 H05/H06/H14/H16/H17 的四级齿轮链、形状、生产进度、基础单位/齿轮攻击和动态发牌；全量 `tests/*.test.mjs` 与 Creator 3.8.8 TypeScript 通过。敌人机制已并入带稳定 `.meta` 的 `NormalLevelRuntime.ts`，不再等待新脚本导入；早/中/晚关新 Web 构建仍需避开当前活动编辑器会话。
- 这一阶段的“可玩”定义为可选择、可进入、按恢复波次战斗并能走胜负状态机，不声明默认零成长账号可以平衡通关每一关，也不声明缺失 Spine/背景已视觉复原。

## 冒烟测试路径

1. 停止当前旧预览，再预览 `assets/scenes/Main.scene`，避免 Creator 继续使用修复前的编译缓存。
2. 确认初始候选只出现在候选栏；把一个齿轮手动拖入已解锁空格，不能覆盖中央能量核心。再把不同齿轮拖到该占格上，旧齿轮应整件退回候选栏；把已放齿轮拖出有效网格也应退回候选栏。横二格、纵二格和纵三格齿轮应逐格覆盖对应的 `100 × 100` 单元。
3. 将同阶同类齿轮拖到一起，候选栏内和背包内都应能合成；拖动件消失，目标件显示下一等级且保持原占格形状。
4. 点击普通刷新：本局第一次应为免费；再次刷新在金币不足 15 时应被阻止。广告刷新每个准备回合只能使用一次。
5. 点击“开始第 1 波”，确认未摆放候选被清空、背包仍在下方；开战时没有免费英雄，只有与核心连通的齿轮随核心转动增加进度，满 100 后才产出。
6. 每波清怪后确认返回布阵并出现新候选；继续至第 15 波。
7. Boss07 被击败后应出现“关卡胜利”，点击“重新挑战”应回到第 1 波、`500 HP` 和 `0` 金币。
