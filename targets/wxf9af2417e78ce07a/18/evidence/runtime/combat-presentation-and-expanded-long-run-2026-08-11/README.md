# 战斗表现精修与扩展长跑（2026-08-11）

本轮按竞品原包证据实现齿轮触发旋转：每次动力接触转一整圈，按格子与动力核心索引交替正反向，相邻相位相差 22.5°；基础时长使用原包 `getDelayTime()` 对应的 200ms，并受效率同比缩短。头像、连接底板和产能 UI 保持正向。单位增加原截图可见的椭圆落地阴影，并按 Y 坐标稳定排序。

实机构建验证捕获到齿轮同帧 `active:319.5°` / `idle:22.5°` 的交替状态，15 次动力触发，8 个存活单位均有阴影。辉光只保留触发时的弱边光；不使用与竞品不符的常亮或夸张光圈。

扩展长跑通过显式 `longRunValidation=1` 输入自动化完成。它只代替摆放、同类合成、开战、词条选择和普通关失败重试，不直接修改生命、攻击、波次或阶段。为抵消应用内浏览器后台约 1 FPS 的节流，验证使用 90 倍追帧，但每次仍拆成不超过 50ms 的固定模拟子步；普通游戏入口仍是 1×/1.5×。

已完整通关：

- 1003：10/10 波，累计 501.504 秒，4 次重试，2883/2883 次生产应用，0 缺失。
- 1100：15/15 波，累计 1416.110 秒，16 次重试，8457/8457 次生产应用，0 缺失。
- 1200：15/15 波，累计 993.256 秒，16 次重试，7521/7521 次生产应用，0 缺失。

每日与无尽均完成了真实进入、战斗、失败和结算闭环，但没有达到每日 10 波或无尽 300 秒。默认账号每日坚持 11.309 秒；明确标注的 20 星英雄养成夹具每日坚持 13.529 秒，无尽坚持 10.684 秒。两者都暴露同一缺口：授权证据缺少竞品账号的 P01 动力角色养成与晚期存档/开局产能。为保持竞品一致性，本轮没有用直接改攻击、血量、阶段或波次的方式伪造通过。

完整机器数据见 `manifest.json`。
