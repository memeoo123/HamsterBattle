# Trait package visual comparison — 2026-08-09

The missing `bagLikeBuff` FairyGUI package and its `bagLikeBuff_0` atlas were
recovered from the authorized resources2 bundle. The shared resources3
`image/effect` atlas was also recovered and decoded for the three trait choices
visible in the original level-1004 reference.

The fresh 750×1334 reconstruction capture now uses the original title ribbon,
card shell, recommendation flag, blue/purple buttons, button icon, purple text
panel and the exact explosion/gun/ice pictograms. FairyGUI coordinates were
converted directly into the reconstruction design space; the card top/bottom
and action-button centers now align with the original after removing the
original screenshot's 88-pixel WeChat host header and normalizing 820→750.

`visualBaseline` remains pending. The trait package's former component-art
blocker is substantially closed, but inline colored description spans and small
font-metric differences remain. The battle pair also still needs a synchronized
elapsed-time/RNG capture, and the preparation/backpack geometry needs its own
final comparison.
