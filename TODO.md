# TODO

## 优先方向：主场景与关卡选择

- [x] 制作主场景基础布局与交互入口。
- [x] 制作关卡选择界面与关卡状态模型。
- [x] 打通“主场景 → 选择关卡 → 进入战斗 → 返回”的完整流程。
- [x] 为首批可选关卡补充回归测试和运行证据。

当前已开放恢复表 `1001..1200` 全部 200 关；视觉一致性不属于本阶段开放门槛。

- [x] 合入版本化账号成长与关卡推进：英雄解锁/星级、挑战次数、资源奖励、碎片、正式升星和通关进度均可持久化。
- [x] 最终视觉门禁已由 2026-08-10 的特质与固定战斗基线通过；相关截图和 manifest 已归档。

## 优先方向：200 关批量接入

- [x] 生成可重复构建的全关卡依赖矩阵（JSON、CSV、Markdown）与一致性测试。
- [x] 将准备阶段从 `PREPARATION_CONFIGS` 逐关白名单改为共享表驱动；永久静态/首挑战批次读取原表 `staticBricks`，逐波金币读取 `coinRewards`。
- [x] `fightscene_02`、`fightscene_04` 采用明确的显示回退，机制阶段不再阻塞；原图恢复留到视觉阶段。
- [x] 接通 25 种实际出场敌人的表驱动基础机制及专注、自爆、击退、刺杀和大型 Boss 主动技。
- [x] 恢复推荐阵容缺失的 `H05/H06/H14/H16/H17` 四级齿轮、形状、生产进度、基础攻击与动态发牌。
- [x] 用依赖矩阵验证全部 200 关：当前 `200 verified / 0 blocked`。
- [x] 将逐波/终局状态机接入生产消费者，并对 200 关全部 `2,978` 波验证下一波、末波胜利、失败、重试和胜利清零补偿。
- [x] 穷举首次动态发牌全部 `1,728` 种三英雄组合，并验证 16 个静态首批关卡均至少有一个可摆放且直接接触核心的生产齿轮；生产渲染/占格/连通统一消费恢复形状。
- [x] 新会话默认使用首次挑战数据（`challengeTimes=1`），有关卡静态批次时优先消费原表 `staticBricks`。
- [x] 对 `1002/1003` 完成正式表驱动配置、独立候选入口、新鲜 Web Mobile 构建、准备拖放与第 1 波启动冒烟（`28/28` 浏览器断言）。
- [x] 修复实际 Web 构建仍只显示 2 关的问题：主页选关现为 20 页 × 10 关，真实点击 19 次下一页后可从第 20 页进入第 200 关；返回主页后再次选关会回到第 1 页。
- [x] 为所有生产齿轮增加不依赖头像贴图的独立黄色产兵条；新构建中 `1001/1100/1200` 真实拖放开战后分别显示 60%/50%/42%，触发与应用一致、缺失配置为 0。
- [x] 修复竖向两格 H11 治疗齿轮无头像，并从原始 `heroSmallHead` 图集补齐 H05/H06/H14/H16/H17；第 5 关静态首批验证所有生产候选均有可见头像。
- [x] 启用后三种五级融合 `H1005/H1505/H1805`：已接通配方星级门槛、精确形状/工人进度、头像、生产者 profile、战斗单位和技能链；H10 五段全场主动技、H18 六段前向矩形主动技、H15 击杀/回合金币均已进入真实战斗。浏览器夹具确认三枚头像及产兵条加载、两种仓鼠完成生产、主动技命中、H15 击杀金币生效且无缺失配置。
- [x] 核对 H1005 普攻 `type=11` 弹体边界：已证明 type 11 不属于 Ray（Ray=18），而是回退到默认 `BulletUnit`；现按 `atkPoint=(0,50)`、固定初始方向、速度 1000、目标点前约 20 像素停止、单次 `100×300` 矩形伤害和锁定目标死亡不补伤害运行。`distance/timeLimit/width/interval/autoLock` 在该版本路径中均未被消费，不是待补机制。
- [x] 将缺失的 `fightscene_02` / `fightscene_04` 在加载前稳定回退到已有背景，避免中晚关战场黑屏。
- [x] 完成 1001 全 5 波正式胜利闭环：连续会话包含 6 次失败/重试、固定 H02 天赋、机制优先补偿、全部波次与最终胜利；胜利清零失败次数，879/879 次生产应用、0 缺失配置、0 运行错误。
- [x] 完成 15 波中/晚关长跑：1100 证明旧 10% 下限在 25 次失败后仍不可靠；保留原表及前 3 次精确补偿，把重建专用长期下限改为 1% 后，新 Web 构建从零累计到第 17 次并正常完成全部 15 波，胜利清零失败计数。获胜段峰值己方 9、敌方 22，0 运行错误、0 缺失生产配置；视觉一致性不作为本阶段阻塞。
- [x] `1002` 已完成无改值的全 8 波正式胜利闭环：连续会话累计 5 次失败后，第 6 次挑战以 361/1375 HP 通关；生产、特质、失败补偿、结果页重试和胜利清零链全部闭合，缺失产兵配置与运行错误均为 0。该结果证明机制优先补偿可在默认重建账号上收敛，但不替代原版目标账号成长取证。
- [x] 恢复终局结算顺序：到期弹丸先于胜负判定，基地死亡优先；最后击杀后立即清除未来弹丸，EXP 同步结算，回合奖励延迟 1000 ms 发放。
- [x] 确认失败断点生命周期：未完成战斗可恢复 `failedTimes`，但失败/结束/退出会清除断点；外部账号层失败补偿不作为战场跨进程存档实现。
- [x] 取得 `GameTimer` 核心回调与 `BattleTimer` 战斗帧同一引擎 tick 的跨调度器先后证据：公共 Timer、`BaseLauncher` 与 Cocos Scheduler 源码共同确认同 tick 为核心生产先、战斗帧后，并由 32 条生产契约锁定。

## 延后处理：账号与外围机制高保真

以下项目不再阻塞当前 200 关机制基线和视觉阶段，后续取得账号/平台证据时再恢复：

- [ ] 取得目标竞品账号的英雄解锁集合、H01/H12 等英雄星级及 P01 成长值，并复现该账号的候选过滤、特质资格、五级融合门槛和精确平衡。
- [ ] 恢复“换一批”“全都要”的真实广告播放与完成/失败/取消回调；当前确定性选择逻辑保持可测试替代入口。
- [ ] 继续恢复剩余账号相关 `condition/verify` 分支，并区分包内规则与外部存档输入。
- [ ] 核对是否还有会打断或包围活动波次的外围弹窗阶段，并补充暂停/恢复契约。
- [ ] 对覆盖退回、主动取下、非法落点、合成位置、每回合刷新计数完成同状态原版交互复验；当前重建行为已有自动化契约和可玩实现。

## 延后处理：后下载战斗 Spine 取证

以下项目等待实际游玩到后期兵种时再逆向，不把当前包内缺失资源误判为原版不存在：

- [ ] 在原版首次遇到后期兵种前后保存资源目录、下载请求和版本索引快照，确认 Spine 是否由运行时按关卡后下载，并按 AppID/版本隔离证据。
- [ ] 取证并恢复当前未导入工程的我方模型：H05、H06、H16 的 1–4 级，以及五级融合 H10、H18；保留 `.skel/.atlas/.png` 原件、逻辑路径、哈希和 Spine 版本。
- [ ] 复核 200 关实际出场的 25 种敌人：当前只有 M02/M03/M07 与 Boss02/Boss03/Boss07 在战斗配置中直接接入 Spine；其余 19 种虽然视觉目录已有资源，但仍需验证原版下载来源并接入真实战斗映射。
- [ ] 增加运行时兵种视觉门禁：逐种真实生成并记录 Spine 加载成功/失败，任何实际出场兵种使用 `FallbackUnit` 都必须显式报告，不能再用视觉目录展示替代战场验收。

依赖摘要位于 `targets/wxf9af2417e78ce07a/18/generated/normal-level-dependency-matrix.md`，逐关阻塞字段位于同名 `.csv` / `.json`。

## 当前优先级：Visual Baseline 精修

- [x] 从原包绑定特质标题飘带、卡片外框、推荐角标、三张参考能力图标与蓝紫装饰按钮，并形成新鲜构建截图证据。
- [x] 完成特质说明文字内联着色、原表断行与字体位置微调；新鲜 750×1334 构建截图和 21 条表现契约已通过。
- [x] 按标准化视口校准准备/背包区、棋盘、候选区和底部控制区的整体几何；恢复“获取格子 ×3”、刷新状态与原版按钮文案，并移除遮挡货币栏的重建主页按钮。
- [x] 核对多格齿轮连接片与准备态局部美术：`panel1..4`、全部原表旋转和 `cl1..cl5` 裁切均已锁定；修正 `cl1` atlas Y 坐标 1 像素误差。
- [x] 在匹配的运行时长和随机事件边界重新取证战斗帧：按 `100401` 排期冻结于 4.010 秒、锁定四只怪及 863 HP 发展态；正常运行仍使用原生随机出生。战场/背包分界、HP 条和面板顶边完成宿主头部归一化对齐。
- [x] 把背包 HP 的文字心形替换为原包 `xl_icon.png` 红色心形图标（`439,374,32×27`），并按 `HpShieldBar` 原坐标复验数字字体、描边与间距。
- [x] 对齐普通战斗伤害数字：恢复 `resources2/ui/battleNum` 原始位图字形、无负号数值格式、1.3 初始缩放，以及 `t` 的 46 px 上浮与 0.3–1.0 秒淡出时间线；同场景截图和 25 条专项契约通过。
- [x] 对齐 H13 基础命中特效和同一伤害帧：恢复 `H13_S1_LOWER / baomihua_hill` Spine、`pskill01` 与原版低层路由，首击/弹跳命中和伤害数字同一更新触发；冻结夹具仍明确保留账号属性导致的 23/27 数值差异。
- [ ] （低优先级）继续核对战斗态齿轮高亮/辉光和单位阴影、层级、颜色，扩充逐帧成对截图。

当前 `check:visualBaseline` 已通过；以上未完成项属于后续表现精修，不阻塞 200 关机制、账号闭环或当前完成状态。
